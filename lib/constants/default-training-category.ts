/**
 * Stable UUID for the seeded "Homecare" category (see supabase migrations).
 * Used as DB DEFAULT / legacy backfill and as the app fallback when no category is posted on create.
 */
export const DEFAULT_TRAINING_CATEGORY_ID = "b1111111-1111-1111-1111-111111111101";
